# Set dribble permissions in parallel

Sets permissions of dribbles in parallel

## Usage

``` r
set_permissions_parallel(dribble_permissions)
```

## Arguments

- dribble_permissions:

  `tibble` A permission table with associated dribbles.

## Value

`NULL`

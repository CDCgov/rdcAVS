# Function to safely check if a character is ASCII

Function to safely check if a character is ASCII

## Usage

``` r
is_ascii_char(char)
```

## Arguments

- char:

  `chr`

## Value

`bool` Whether it is an ASCII character or not

# Convert column number to column name.

Convert column number to column name.

## Usage

``` r
num_to_col(n)
```

## Arguments

- n:

  `int` Positive integer corresponding to column location. (1 = "A").

## Value

`str` The column name in A1 format.

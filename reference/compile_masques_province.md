# Compile masque at the province level

Compile masque at the province level

## Usage

``` r
compile_masques_province(campaign_name)
```

## Arguments

- campaign_name:

  `str` Name of the campaign.

## Value

NULL, invisibly.

# Checks the folder size

Check the folder size in GB.

## Usage

``` r
check_folder_size(folder_path)
```

## Arguments

- folder_path:

  `str` Path to the folder.

## Value

`int` Size of the folder in GB.

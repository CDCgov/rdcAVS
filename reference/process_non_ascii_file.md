# Resolve non-ASCII character issues

Resolve non-ASCII character issues

## Usage

``` r
process_non_ascii_file(file_path, output_path)
```

## Arguments

- file_path:

  `str` Path to file where non-ASCII characters are located.

- output_path:

  `str` Output path

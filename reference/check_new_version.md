# Checks whether there is a new version of the app on GitHub

Checks whether there is a new version of the app on GitHub

## Usage

``` r
check_new_version(local_version = get_app_version())
```

## Arguments

- local_version:

  `str` Local version number

## Value

`str` Message if new version is available otherwise NULL.

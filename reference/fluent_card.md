# Fluent-style Card Container

Creates a simple card container with Fluent UI

## Usage

``` r
fluent_card(...)
```

## Arguments

- ...:

  UI elements to include inside the card.

## Value

A `shiny.tag` `div` containing the wrapped content.
